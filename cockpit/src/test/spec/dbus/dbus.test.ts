import 'jasmine'
import { registerStart } from '../libs/helpers'
import { dispatch } from 'auctrix'

describe('subscription-manager DBus Object testing with cockpit', function() {

    
    
    it('Creates a unix socket when we call the Register service', registerStart)
})
